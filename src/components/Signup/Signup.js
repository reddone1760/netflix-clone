import React from "react";
import "./Signup.css";

function Signup() {
  return (
    <div className="signUp__Screen">
      {/* <form>
        <h1>Log in</h1>
        <input ref={emailRef} type="email" placeholder="Email" />
        <input ref={passwordRef} type="password" placeholder="Password" />
        <button type="submit" onClick={register}>
          Sign Up
        </button>

        <h4>
          <span className="signup__Screen__grey">New to Netflix?</span>
          <span
            onClick={(e) => {
              signin(e);
              setIsLogged(true);
            }}
            className="signup__Screen__link"
          >
            Sign Up now.
          </span>
        </h4>
      </form> */}
    </div>
  );
}

export default Signup;
